version: "3.9"

services:
  bot1:
    image: myapp
    mem_limit: 1500m
    restart: always
    networks:
      - duo_network
  bot2:
    image: myapp
    mem_limit: 1500m
    restart: always
    networks:
      - duo_network
  bot3:
    image: myapp
    mem_limit: 1500m
    restart: always
    networks:
      - duo_network
  bot4:
    image: myapp
    mem_limit: 1500m
    restart: always
    networks:
      - duo_network
  bot5:
    image: myapp
    mem_limit: 1500m
    restart: always
    networks:
      - duo_network
  
networks:
  duo_network:
    driver: bridge